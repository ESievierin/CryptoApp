<Page x:Class="CryptoApp.Presentation.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:converters="clr-namespace:CryptoApp.Presentation.Converters"
      Title="CryptoApp"
      Background="#121212"
      Foreground="White">

    <Page.Resources>
        <converters:PriceWithCurrencyConverter x:Key="PriceWithCurrencyConverter"/>
    </Page.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadCoinsCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <DockPanel Margin="0,0,0,10">
                <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                         Width="250"
                         Height="25"
                         Background="#1E1E1E"
                         Foreground="White"
                         BorderBrush="#2A2A2A"
                         BorderThickness="1"
                         FontSize="14"
                         KeyDown="SearchTextBox_KeyDown"/>

                <Button Content="Search"
                        Command="{Binding SearchCoinsCommand}"
                        Width="100"
                        Margin="10,0,0,0"
                        Background="#00E676"
                        Foreground="Black"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand"/>

                <ComboBox ItemsSource="{Binding AvailableCurrencies}"
                  SelectedItem="{Binding SelectedCurrency, Mode=TwoWay}"
                  Width="100"
                  Margin="20,0,0,0"
                  Background="#1E1E1E"
                  Foreground="Black"
                  BorderBrush="#2A2A2A">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Code, Converter={StaticResource UpperCaseConverter}}"
                       Foreground="Black"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Button Content="Converter"
                        Command="{Binding OpenConverterCommand}"
                        Width="120"
                        HorizontalAlignment="Right"
                        Margin="20,0,0,0"
                        Background="#8A00C4"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </DockPanel>

            <Border Background="#121212"
                    BorderBrush="#2A2A2A"
                    BorderThickness="1"
                    CornerRadius="6"
                    MaxHeight="200"
                    Margin="0,0,0,15">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSearchResultsVisible}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding SearchResults}"
                                  ItemTemplate="{StaticResource SearchCoinCardTemplate}">
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <EventSetter Event="MouseLeftButtonUp"
                                             Handler="SearchResult_Click"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <ItemsControl ItemsSource="{Binding Coins}"
                          ItemTemplate="{StaticResource CoinCardTemplate}"/>
        </StackPanel>
    </ScrollViewer>
</Page>
